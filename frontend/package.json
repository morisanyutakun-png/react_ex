{
  "name": "react-fastapi-demo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "setup": "rm -rf node_modules package-lock.json && npm cache clean --force && npm install --omit=optional",
    "dev:fix": "npm run setup && npm run dev",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^7.3.1"
  },
  "optionalDependencies": {
    "@rollup/rollup-darwin-arm64": "^4.57.1"
  }
}
